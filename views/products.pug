extends layout

block content
  div(class='d-flex flex-column justify-content-center') 
    each item in item_list
      a(href=`/products/${item._id}`)= item.name 
      p= item.price + ' gold, ' + 'Qty Available: ' + item.number_in_stock + ', ' + item.category.name